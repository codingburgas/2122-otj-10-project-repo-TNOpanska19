#pragma once

#include "pch.h"
#include "../pm.types/user.h"

namespace projectsMenu
{
	void projectsManagementView(pm::types::User activeUser);

	void createProjectMenu(pm::types::User activeUser);
}